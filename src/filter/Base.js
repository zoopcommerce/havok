define([
    'dojo/_base/declare',
    'dojo/Stateful'
],
function(
    declare,
    Stateful
){
    // module:
    //		havok/filter/Base

    return declare(
        [Stateful],
        {
        }
    );
});
